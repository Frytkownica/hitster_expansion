name: Deploy GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Write runtime config
        run: |
          cat > config.js <<'EOF'
          window.HITSTER_CONFIG = {
            supabaseUrl: "${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}",
            supabasePublishableKey: "${{ secrets.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY }}"
          };
          EOF

      - name: Prepare Pages artifact
        run: |
          mkdir -p _site/vendor
          cp index.html _site/
          cp styles.css _site/
          cp app.js _site/
          cp config.js _site/
          cp vendor/textFit.min.js _site/vendor/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
